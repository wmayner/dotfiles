# get environment
source ~/.env
# get aliases
source $ALIASES
# get bash-specific aliases
source $BASH_ALIASES
# use vi to edit commands
set -o vi

[[ -s "$HOME/.rvm/scripts/rvm" ]] && source "$HOME/.rvm/scripts/rvm" # Load RVM into a shell session *as a function*
