# Modern Python tooling (2026)
# Install via: pipx install <package> (preferred) or pip install -r requirements.txt

# Fast linting and formatting (replaces flake8, isort, yapf, pylint)
ruff

# Ultra-fast package installer (pip replacement)
uv

# Testing
pytest

# Utilities
pip-autoremove
tqdm
pygments

# Note: The following have been replaced by modern alternatives:
# - virtualenvwrapper → Use 'uv venv' or native Python venv
# - powerline-status → Using Starship prompt instead
# - flake8 → Replaced by ruff
# - isort → Replaced by ruff
# - yapf → Replaced by ruff format
# - pylint → Most checks covered by ruff
